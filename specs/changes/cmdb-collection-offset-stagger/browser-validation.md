# 本地 Chrome 验证记录

日期：2026-09-11，16:15 起（北京时间）；包含后续新建任务闭环验证。
入口：`http://localhost:3000/cmdb/assetManage/autoDiscovery/collection?category=network&plugin=network`。
方式：通过用户的 Google Chrome 页面实际点击、填写和查看；登录身份为当前已有的 admin 会话。

## 当前结论

**配置链路通过，真实采集入库未通过验收。** 本轮执行了新建、同一任务编辑、拓扑关闭／重开、周期修改、节点配置核对、手动同步和单目标诊断；没有把失败或无结果算作通过。

后续按用户要求已新建独立任务 **8**：`错峰闭环验证-网络-0911-1624`，并在同一任务上保存改名为 `错峰闭环验证-网络-0911-1624-已编辑`。
目标缩小为 `10.10.69.1–10.10.69.1`，接入点为 `fusion-collector-default`，
设备／拓扑周期均为 2 分钟，设备超时沿用 30 秒，拓扑超时 60 秒。
首次复制未带入可用团体字，保存被后端拒绝（“第 1 组凭据（v2）缺少团体字”）；
填写测试值后创建成功，列表刷新后仍存在。该测试值并未证明目标认证有效。

节点管理子配置总数从 15 增至 17，已通过只读配置编辑器核对：

| 配置身份 | 周期 | 偏移 |
|---|---|---|
| `cmdb_8` | `120s` | `60s` |
| `cmdb_8_topology` | `120s` | `0s` |

改名保存显示“编辑成功”，重新打开两份节点子配置后，上表周期、偏移和任务身份均保持不变。
手动点击任务 8 的“同步”后，页面显示“同步中”并禁用编辑等操作，随后返回“异常”：
“未发现任何有效数据，请检查采集目标连通性、凭据与采集范围配置”。同步时间为 `2026-09-11 16:38:04`，数据上报时间为 `--`。
因此新增、编辑、配置生成已得到实际页面证据，但真实采集入库尚未验收通过。

### 同一任务的拓扑开关与周期修改

1. 关闭任务 8 的“是否采集关系”并保存，再次打开表单仍为关闭状态。
2. 节点管理子配置由 17 份减为 16 份，搜索 network 后由 3 份减为 2 份；任务 8 的设备配置仍为 `cmdb_8`、`120s`、偏移 `60s`。
3. 在任务 8 重新开启拓扑，设备周期改为 30 分钟、拓扑周期使用推荐值 150 分钟，拓扑超时设为 60 秒，保存完成。关闭后再打开会恢复拓扑默认选项；本轮重新设定超时后保存。
4. 从异常提示进入“采集工具”，页面显示“无法自动带入，请手动补全参数”；手工填写相同单目标、同一接入点和相同测试凭据执行连通性诊断。自动带入失败单独记录，尚未归因于 collection_offset。

重开拓扑并修改周期后，节点管理搜索 network 恢复为 3 份（含既有任务的拓扑配置）。只读核对任务 8 的最终配置：

| 配置身份 | 最终周期 | 最终偏移 | 结果 |
|---|---|---|---|
| `cmdb_8` | `1800s` | `900s` | 周期修改已同步，偏移在周期内 |
| `cmdb_8_topology` | `9000s` | `3900s` | 重开后重新生成，偏移在周期内 |

任务 8 保留供复查；最终设备／拓扑周期为 30／150 分钟。上述证据确认节点管理保存的配置，不能替代节点进程实际加载与运行时间戳。

### 实际采集阻塞证据

- 任务详情最终显示：本次发现、需要新增、需要更新、需要删除、拓扑事实均为 0。
- 实际点击“原始数据 (0)”页签，显示“暂无原始数据”；拓扑显示“暂无拓扑事实”。
- 单目标连通性诊断持续显示 Running，最后一次读取计时为 `03:54`，没有结果；随后点击页面“暂停”，界面回到“暂无结果，请执行采集”。该操作只证明页面停止等待，不表示后台任务已取消。
- 浏览器针对 Failed/prefill 的错误日志筛选没有返回错误；这不证明请求或后台执行正常。
- 尚未确定无数据／诊断无返回的根因，不能直接判定为凭据错误、网络不通或 collection_offset 缺陷。本轮未修改与此无关的生产代码。

代码侧补充边界：当前 `CollectModelService._exec_task_after_lock` 在 DEBUG 分支调用最新轮次同步，在非 DEBUG 分支派发立即执行；仅凭页面“同步”按钮和状态变化不能证明节点新执行了一轮采集。本轮未核实运行后端的 DEBUG 值。
此前列表操作点击未能产生可见响应，随后浏览器报告控制连接未附着；不能将此前点击计为成功同步或编辑，也不能直接归因为产品缺陷。
已在同一 Chrome 重新打开任务 8 页面恢复验证。下述“未提交草稿”记录仅指第一轮页面验证，不代表后来没有创建任务。

### 第一轮页面检查结论

页面交互与存量配置查看已验证，新增任务保存和真实采集闭环尚未验证。
现有网络任务已处于“未发现任何有效数据”的异常状态，不能将其作为成功采集的基准。
已请求用户提供可用的单台测试目标及可复用的任务／凭据配置；未读取或要求明文密码。

## 已执行项目

| 项目 | 操作与实际结果 | 结论 |
|---|---|---|
| 页面加载 | 网络采集列表正常显示，已有任务“网络设备采集11”可打开详情 | 通过 |
| 既有任务状态 | 任务 ID 1，异常；提示未发现有效数据，数据上报时间为空，详情新增／更新／删除／拓扑事实均为 0 | 记录基线，不能认定采集成功 |
| 复制表单 | 点击“复制任务”打开新增表单，带入接入点、周期、IP 范围与拓扑选项，任务名称为空 | 通过 |
| 必填校验 | 空任务名提交，显示“请输入任务名称”，没有创建任务 | 通过 |
| 周期联动 | 草稿设备周期由 30 改为 10 分钟；拓扑推荐值由 150 改为 50 分钟，原自定义 150 保留 | 通过 |
| 恢复推荐值 | 点击“恢复推荐值”，拓扑周期变为 50 分钟 | 通过 |
| 拓扑关闭／重开 | 关闭后隐藏拓扑配置；重开后恢复显示并保留 50 分钟 | 通过 |
| 偏移配置入口 | 基本配置及高级配置未增加需要用户填写的偏移项 | 符合设计；不等同于验证服务端分配 |
| 放弃草稿 | 出现未保存提示，确认放弃后返回列表；测试草稿没有提交 | 通过 |
| 主机列表 | 主机插件能切换、加载，当前没有主机采集任务 | 页面通过，未验证主机实际采集 |
| 接入点运行状态 | 节点管理直连区域中 `fusion-collector-default` 显示 9 个组件正常，最后上报时间为 16:19:43 | 页面可见运行状态正常 |
| Telegraf 运行状态 | 详情显示 `Running`，子配置列表正常加载 | 通过 |
| 存量拓扑配置 | 只读打开 network_topo 子配置，`interval = "9000s"`，对应 `cmdb_1_topology`，未包含 `collection_offset`；关闭编辑器未保存 | 符合存量不重推设计 |

## 待完成

1. 使用认证有效且可达的目标，验证设备与拓扑数据进入 CMDB；本次任务同步返回无有效数据。
2. 对主机插件完成同等实际采集验证；至少观察两个业务插件实际周期，确认非零偏移的周期触发时序。

本次没有保存或删除任何既有任务，没有触发原有 `10.10.69.1–255` 范围的采集，没有修改节点配置。
浏览器页面本身不直接展示偏移的数据库字段；后续通过节点管理生成配置和采集结果核对，不能凭页面渲染成功推断链路成功。
