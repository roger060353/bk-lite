# Local RUM Redis — ACL users match gateway/controller/maintainer validators.
# Passwords are contract-fixture only; production secrets must be injected.

port 6379
bind 0.0.0.0
protected-mode no
appendonly yes
appendfsync everysec
dir /data
aclfile /etc/redis/users.acl
