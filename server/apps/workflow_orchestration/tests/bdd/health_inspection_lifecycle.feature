# language: zh-CN
功能: 编排中心 MVP 验收数据与健康巡检报告
  作为运维管理员
  我希望可重复地生成可见、可查询、可下载的 MVP 验收数据
  以便验证健康巡检、人工审批和其他主要原子

  场景: 正常路径 - 重建全部 MVP 流程并得到可渲染的多维巡检数据
    假设 页面查看用户有权访问当前团队
    当 管理员确认重建 MVP 验收数据
    那么 应当生成七条已发布流程和十一条执行记录
    并且 应当有一条待审批记录
    并且 应当同时存在 Word 与 Excel 健康巡检流程
    并且 健康巡检数据应当包含 CPU 内存 磁盘和网络维度
    并且 Excel 和 Word 内置模板都应当能渲染健康巡检数据

  场景: 边界情况 - 未显式确认时拒绝清理与重建
    假设 页面查看用户有权访问当前团队
    当 管理员未确认就重建 MVP 验收数据
    那么 命令应当拒绝执行并且不生成流程

  场景: 边界情况 - 不存在的页面查看用户不能生成数据
    当 不存在的页面查看用户尝试重建 MVP 验收数据
    那么 命令应当拒绝未知用户并且不生成流程

  场景: 边界情况 - 页面查看用户不能跨团队生成数据
    假设 页面查看用户只有其他团队权限
    当 管理员确认重建 MVP 验收数据
    那么 命令应当拒绝跨团队操作并且不生成流程

  场景: 边界情况 - 重复重建只替换 TDD BDD 演示数据
    假设 页面查看用户有权访问当前团队
    并且 当前团队已有一条非演示流程
    当 管理员连续两次确认重建 MVP 验收数据
    那么 非演示流程应当保留且演示数据不应当重复

  场景: 边界情况 - 多磁盘阈值越界应当保留告警证据
    假设 页面查看用户有权访问当前团队
    当 管理员确认重建 MVP 验收数据
    那么 多磁盘告警执行应当标记告警并保留具体磁盘维度

  场景: 边界情况 - Word 与 Excel 巡检流程必须挂载对应模板快照
    假设 页面查看用户有权访问当前团队
    当 管理员确认重建 MVP 验收数据
    那么 Word 与 Excel 巡检流程应当分别挂载 docx 与 xlsx 模板快照
